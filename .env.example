# Postgres connection string.
# Local:    postgresql://postgres:postgres@localhost:5432/bracketbuilder
# Neon:     postgresql://USER:PASSWORD@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
# Supabase: postgresql://postgres.PROJECT:PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/bracketbuilder"

# Anthropic API key, used to research and seed bracket candidates.
# Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY="sk-ant-..."

# Optional. Set to "true" to research with live web search. Off by default:
# search takes ~170s per bracket versus ~30s without, which exceeds the request
# limit on entry-level serverless plans, and costs roughly ten times as much.
# Worth turning on for categories where recency actually matters, if your host
# allows a request that long.
# RESEARCH_WEB_SEARCH="true"

# Optional. Public origin used to build shareable invite links. Leave this unset
# on a normal deploy - the app reads the real host from the request, which is
# what you want behind Vercel or any proxy. Set it only to force a specific
# origin (e.g. a custom domain you front the app with).
# APP_URL="https://brackets.example.com"
